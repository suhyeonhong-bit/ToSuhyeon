# API 키, 안전하게 다루기

README에서 API 키를 "출입증"이라고 설명했죠. 출입증을 아무 데나 흘리고 다니면 남이 주워서 내 이름으로 드나들 수 있는 것처럼, API 키도 유출되면 남이 내 이름으로 데이터를 가져다 쓸 수 있습니다. 무료 API도 사용량 제한이 걸려 있는 경우가 많아서, 키가 새어나가면 내가 정작 필요할 때 막히는 일이 생길 수 있어요.

이 저장소는 실제로 깃허브(GitHub)에 공개돼 있습니다. 그래서 API 키를 스크립트 안에 그냥 적어두면, 커밋하고 푸시하는 순간 그 키가 인터넷에 그대로 공개됩니다. 이 문서는 그런 일을 막는 방법을 알려드립니다.

## `.env` 파일이 뭔가

API 키를 스크립트 코드 안에 직접 적지 않고, 따로 떼어서 보관하는 파일입니다. 코드는 이 파일을 몰래 들여다보고 값만 꺼내 쓰는 식으로 동작해요. 이렇게 분리해두면 코드는 깃허브에 올라가도 되지만, `.env` 파일만 안 올라가게 막으면 키는 노출되지 않습니다.

## 만드는 법

1. **프로젝트 폴더에 `.env`라는 이름의 파일을 만듭니다.** 확장자가 없는 파일이에요. AI에게 "`.env` 파일 만들어줘"라고 하면 만들어 줍니다. 이 단계에는 아직 키 값을 적지 않으니 안전합니다.

2. **그 안에 `이름=값` 형식으로 키를 직접 적습니다.**

   > **이 키 값은 절대로 클로드 코드·코덱스 같은 에이전트 대화창에 입력하면 안 됩니다.** "내 API 키는 OOO야, `.env`에 넣어줘"라고 채팅창에 치는 순간, 그 키가 대화 기록에 그대로 남아버립니다. 반드시 VS Code 왼쪽 탐색기에서 `.env` 파일을 직접 더블클릭해서 열고, 아래처럼 값을 손으로 입력한 다음, `Cmd+S`로 저장하고 파일을 닫으세요.

   ```
   FRED_API_KEY=여기에_발급받은_키를_붙여넣기
   ECOS_API_KEY=여기에_발급받은_키를_붙여넣기
   ```

3. **`.gitignore` 파일에 `.env`를 한 줄 추가합니다.** `.gitignore`는 "이 파일들은 깃이 못 본 척해라"라고 적어두는 목록이에요. 여기에 `.env`를 적어두면, 아무리 커밋하고 푸시해도 이 파일만은 깃허브에 절대 올라가지 않습니다. 이것도 AI에게 "`.gitignore`에 `.env` 추가해줘"라고 하면 됩니다.

이 세 가지는 프로젝트를 시작할 때 딱 한 번만 해두면 됩니다. AI에게 "API 키는 `.env` 파일에서 읽어오게 스크립트 짜줘"라고 하면, 나머지는 알아서 처리해줍니다.

## 만약 이미 올려버렸다면

`.env`를 안 만들고 키를 코드에 직접 적었다가 커밋·푸시한 걸 뒤늦게 알았다면, **파일에서 키를 지우는 것만으로는 부족합니다.** 한 번이라도 깃허브에 올라갔던 값은 이미 유출된 걸로 간주해야 합니다. 지운 뒤에도 과거 기록(히스토리)에는 남아 있을 수 있어서요.

이럴 땐 그 키를 발급받은 홈페이지(ECOS, FRED 등)에 다시 가서 **키를 재발급(기존 키 폐기 후 새로 발급)** 받으면 됩니다. 어떻게 하는지 모르겠으면 AI에게 물어보거나 저한테 말씀해주세요.

## 한 줄로 기억할 것

**API 키는 비밀번호와 같습니다.** 코드에 직접 적지 말고, `.env`에 넣고, `.gitignore`로 가려두세요.

**그리고 키 값 자체는 에이전트 대화창에 절대 입력하지 마세요.** 파일 경로로 직접 들어가서 값을 적고, 저장(`Cmd+S`)하고 나오면 됩니다.
